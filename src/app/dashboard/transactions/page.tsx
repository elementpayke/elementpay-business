import PlaceholderPage from "@/components/dashboard/PlaceholderPage";

export default function TransactionsPage() {
  return (
    <PlaceholderPage
      title="Transactions"
      description="A dedicated transaction operations view will live here with filters, exports, and settlement tracking. This placeholder keeps navbar routing complete."
    />
  );
}
import PlaceholderPage from "@/components/dashboard/PlaceholderPage";

export default function DeveloperPage() {
  return (
    <PlaceholderPage
      title="Developer"
      description="Developer keys, webhook logs, and API integration details can plug into this route while keeping the top-level app shell stable today."
    />
  );
}
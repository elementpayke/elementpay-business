import PlaceholderPage from "@/components/dashboard/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Account Settings"
      description="Workspace settings, billing controls, and notification defaults can be expanded here from the avatar dropdown entry point."
    />
  );
}
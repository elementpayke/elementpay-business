import PlaceholderPage from "@/components/dashboard/PlaceholderPage";

export default function SecurityPage() {
  return (
    <PlaceholderPage
      title="Security"
      description="This page can be extended with audit logs, passkey management, and policy controls without reworking the current shell."
    />
  );
}